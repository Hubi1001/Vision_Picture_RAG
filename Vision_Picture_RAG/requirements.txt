SQLAlchemy
psycopg2-binary
pgvector[sqlalchemy]
# Embedding / LLM dependencies
sentence-transformers
transformers
accelerate
# PyTorch z obsługą CUDA (dla GPU)
torch>=2.0.0
torchvision>=0.15.0
# Jeśli masz GPU NVIDIA, zainstaluj: pip install torch --index-url https://download.pytorch.org/whl/cu118
Pillow
# Dodatkowe przyspieszenie
bitsandbytes  # Optymalizacja modeli na GPU